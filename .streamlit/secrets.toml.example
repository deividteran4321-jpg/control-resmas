# Copia este archivo como "secrets.toml" para desarrollo local con Postgres
# (ese archivo NO se sube a git), o pega su contenido en:
# Streamlit Community Cloud -> tu app -> Settings -> Secrets

DATABASE_URL = "postgresql+psycopg2://usuario:password@host:5432/nombre_bd"
